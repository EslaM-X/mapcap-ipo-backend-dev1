{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "module": "ESNext",                                  /* Specify what module code is generated. Using ESNext to align with "type": "module". */
    "lib": ["ESNext", "DOM"],                            /* Specify a set of bundled library declaration files that describe the target runtime environment. */
    "moduleResolution": "node",                          /* Specify how TypeScript looks up a file from a given module specifier. */
    
    /* Path Configurations */
    "rootDir": "./",                                     /* Specify the root folder within your source files. */
    "outDir": "./dist",                                  /* Specify an output folder for all emitted files. */
    "baseUrl": ".",                                      /* Specify the base directory to resolve non-relative module names. */
    
    /* Strict Type-Checking Options (Audit-Ready) */
    "strict": true,                                      /* Enable all strict type-checking options to ensure maximum code reliability. */
    "noImplicitAny": true,                               /* Enable error reporting for expressions and declarations with an implied 'any' type. */
    "strictNullChecks": true,                            /* When type checking, take into account 'null' and 'undefined'. */
    
    /* Interoperability & Compatibility */
    "esModuleInterop": true,                             /* Emit additional JavaScript to ease support for importing CommonJS modules. */
    "skipLibCheck": true,                                /* Skip type checking all .d.ts files to speed up compilation. */
    "forceConsistentCasingInFileNames": true,            /* Ensure that casing is correct in imports for cross-platform stability (Linux/Windows). */
    
    /* Advanced Functionality */
    "resolveJsonModule": true,                           /* Allow importing .json files, which is essential for reading package.json or config files. */
    "allowJs": true,                                     /* Allow JavaScript files to be a part of your program. Essential for gradual migration. */
    "sourceMap": true                                    /* Create source map files for emitted JavaScript files to aid debugging. */
  },
  "include": [
    "**/*.ts",                                           /* Include all TypeScript files in the project. */
    "server.ts"                                          /* Explicitly include the entry point orchestrator. */
  ],
  "exclude": [
    "node_modules",                                      /* Exclude dependency directories. */
    "dist",                                              /* Exclude the output directory. */
    "test"                                               /* Exclude test files for now to focus on core logic migration. */
  ]
}

