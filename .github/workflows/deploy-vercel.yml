name: MapCap IPO Vercel Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: 🚀 Deploy to Vercel
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy Production to Vercel
        env:
          # بنسيب الـ IDs هنا عشان الـ CLI يربطهم ببعض أوتوماتيك
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          # وبنبعت الـ Token هنا عشان يتخطى مرحلة الـ Login
          PROD_URL=$(vercel deploy --prod --yes --token ${{ secrets.VERCEL_TOKEN }})
          echo "PROD_URL=$PROD_URL" >> $GITHUB_ENV
          echo "🚀 Production deployed: $PROD_URL"

      - name: Show Vercel deployment summary
        run: |
          echo "### 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Production URL:** $PROD_URL" >> $GITHUB_STEP_SUMMARY
