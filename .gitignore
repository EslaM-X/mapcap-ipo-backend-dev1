# --- [DEPENDENCY DIRECTORIES] ---
node_modules/
jspm_packages/

# --- [ENVIRONMENT VARIABLES] ---
# Very critical: Never commit the actual .env to version control
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- [TYPESCRIPT / BUILD OUTPUT] ---
# Exclude compiled JS files from the production build directory
dist/
build/
*.tsbuildinfo
# Optional: if you want to ignore compiled declarations
# *.d.ts 

# --- [LOGS & DIAGNOSTICS] ---
# Excluding the MapCap audit logs to keep the repo clean
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- [SYSTEM & OS METADATA] ---
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini

# --- [EDITOR & IDE CONFIG] ---
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- [BUILD & TEMP] ---
.cache/
.npm/
